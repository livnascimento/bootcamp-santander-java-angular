function soma(x: number, y: number): number {
    return x + y;
}

const resultadoSoma: number = soma(3, 4);

const somaArrow = (x: number, y: number): number => {
    return x + y;
}

const resultadoSomaArrow: number = soma(3, 4);